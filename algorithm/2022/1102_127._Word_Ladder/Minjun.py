class Solution:
    def ladderLength(self, beginWord: str, endWord: str, wordList: List[str]) -> int:
        # trivial case
        if endWord not in wordList:
            return 0
        
        l = len(beginWord)
        
        for i in range()
        for i in range(l):
            
            
        
        a = 'abc'
        b = 'abd'
        print(a[1])
        
        
        # def bfs(b):
             
