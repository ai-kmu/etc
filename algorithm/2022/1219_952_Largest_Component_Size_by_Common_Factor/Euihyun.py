더 풀어보겠습니다...
class Solution(object):
    def largestComponentSize(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        
        n = len(nums)
        temp = []
        check_list = [False for i in range(len(nums))
        #소수 판별   
        def check_(n):
                      
        for i in range(n):
            for j in range(i+1,n):
            #소수 판별
                check_
